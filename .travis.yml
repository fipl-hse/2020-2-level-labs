language: python

python: '3.8'

os: linux

dist: xenial

cache: pip

install:
  - pip3 install -r requirements.txt

stages:
<<<<<<< HEAD
=======
  # - pr_name_check
>>>>>>> 65e0dc7... changed asserTrue to assertGreater, off PR_name_check for now, fixed imports
  - test
  - lint
  - start_py

jobs:
  include:
    # - stage: pr_name_check
      # script:
        # - python3 config/check_pr_title.py --pr-name $(echo ${PR_TITLE// /_})
    - stage: test
      script:
        - bash config/run-tests.sh
    - stage: lint
      script:
        - bash config/run-lint.sh
    - stage: start_py
      script:
        - bash config/run-start.sh

notifications:
  email:
    on_success: always
    on_failure: always
