language: python

python: '3.8'

os: linux

dist: xenial

cache: pip

before_install:
  - git lfs install

before_script:
  - git lfs pull

install:
  - pip3 install -r requirements.txt

stages:
<<<<<<< HEAD
  - pr_name_check
=======
  # - pr_name_check
>>>>>>> ea073a32e1d8cedb1d4b634d9cc0983e1461b8ef
  - test
  - lint
  - start_py

env:
  global:
    - PR_TITLE=$(curl https://github.com/${TRAVIS_REPO_SLUG}/pull/${TRAVIS_PULL_REQUEST} | grep "title" | head -1)

jobs:
  include:
    - stage: pr_name_check
      script:
<<<<<<< HEAD
        - python3 config/check_pr_title.py --pr-name $(echo ${PR_TITLE// /_})
=======
        - if [[ ${TRAVIS_PULL_REQUEST} != "false" && $PR_TITLE != *"[skip_name]"* ]]; then
            python3 config/check_pr_title.py --pr-name $(echo ${PR_TITLE// /_});
          fi
>>>>>>> ea073a32e1d8cedb1d4b634d9cc0983e1461b8ef
    - stage: test
      script:
        - bash config/run-tests.sh
    - stage: lint
      script:
        - bash config/run-lint.sh
    - stage: start_py
      script:
        - bash config/run-start.sh

notifications:
  email:
    on_success: always
    on_failure: always
